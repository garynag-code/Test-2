# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tool output — may contain private chat data
chatgpt_export_output/
chatgpt-chats-for-claude.md
upload-*.zip
upload-*.json

# ChatGPT export archives (may contain private data)
*.zip
conversations.json

# Node / Cloudflare Worker (worship_roster/server)
node_modules/
.wrangler/
# Never commit secrets (VAPID private key, etc.)
.dev.vars
*.local
