# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tool output — may contain private chat data
chatgpt_export_output/
chatgpt-chats-for-claude.md
upload-*.zip
upload-*.json

# ChatGPT export archives (may contain private data)
*.zip
conversations.json

# Perch booking ledgers — these hold real guest names, emails and phone
# numbers, so they must never be committed.
perch.db
*.db
*.db-wal
*.db-shm

# Created by the Start files
.perch-log.txt
.perch-env/
