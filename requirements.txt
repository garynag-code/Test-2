# Web portal, and Perch (the bed & breakfast booking manager, `python -m bnb`)
Flask>=2.3.0

# (Everything else — the ChatGPT parsing/rendering, the CLI, and Perch's
# booking ledger, iCal sync and channel engine — uses only the Python
# standard library.  Flask is needed only to serve the browser apps.)
