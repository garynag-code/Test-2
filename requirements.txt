# Web portal
Flask>=2.3.0

# Google Drive import
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# (Core parsing/rendering and the CLI's local mode use only the standard
# library — these dependencies are only needed for the portal and the
# Google Drive upload step.)
